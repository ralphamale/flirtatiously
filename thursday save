larger themes

-popup modals.
-Flash Errors on modal. /// on screen //
-Display blank image if its blank!!!!

<%= profile.photos.first.file.url(:featured) %>
^^ CUSTOM METHOD.

photos.first.

.exists?


if URI(request.referer).path == '/adsense'
fail in the controller.
^look at request.

